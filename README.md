# Chatgpt SQL Generator

Generate SQL from plain English with Chatgpt

### Installation

```commandline
pip install -r requirements.txt
```

```commandline
python -m ipykernel install --user --name chatgpt-sql-generator --display-name "chatgpt-sql-generator"
```

